// TODO: 2D fallback portfolio - implement in fallback-2d task

export function mountFallback2D(): void {
  // TODO: implement in fallback-2d task
  const app = document.getElementById('app')
  if (app) {
    app.innerHTML = '<p>Loading fallback portfolio...</p>'
  }
}

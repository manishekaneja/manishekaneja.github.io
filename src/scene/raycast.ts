// TODO: raycaster + hover + click - implement in raycast-picking task
import type { RoomId } from './house'

export type RoomSelectedHandler = (roomId: RoomId) => void

export function initRaycast(_onRoomSelected: RoomSelectedHandler): void {
  // TODO: implement in raycast-picking task
}
